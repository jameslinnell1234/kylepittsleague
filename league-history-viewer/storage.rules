rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can read files by URL; writes disabled (use Console or later add auth).
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
